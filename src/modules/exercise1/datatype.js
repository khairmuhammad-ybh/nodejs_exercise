/**
 *
 * Create a function to return different datatype:
 * Function name as follows (use camelCase)
 * 1. booleanFunc
 * 2. numberFunc
 * 3. stringFunc
 * 4. objectFunc
 *
 * open terminal and run this command to test functions "npm run test:datatype"
 */
const datatypeFunc = {
  booleanFunc: () => {
    return true;
  },
  //Add your code here
};

module.exports = datatypeFunc;
