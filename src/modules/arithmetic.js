/**
 *
 * Create a function to perform basic arithmetic operation:
 * Function name as follows (use camelCase)
 * 1. addition
 * 2. subtraction
 * 3. multiplication
 * 4. division
 *
 * open terminal and run this command to test functions "npm run test:arithmetic"
 */

const arithmeticFunc = {
  addition: (a, b) => {
    return a + b;
  },
  //Add your code here
};

module.exports = arithmeticFunc;
